# Demo 21: the GPU scheduler's batch buffer. route_p2c at 1.2x rated capacity (2,254 rps against
# the 256-replica fleet's rated 1,878 at a batch of 256), so every replica runs a queue and what the scheduler admits
# into a step, and what the router steers by, is the whole difference. Three files, one change each.
# The same buffer under weighted_random: each replica weighs 1 - 0.3 for any queued decode - 0.5 for
# any queued prefill - 0.05 per decode item beyond the open buffer - 0.1 per prefill item beyond it,
# clamped at zero, over the stale view; the router draws in proportion.
name = buffer_batch_weighted
seed = 20260906
duration_s = 120
warmup_s = 15

# Fleet. A 70B-class model on 8xH100 per replica; step_base_ms is calibrated in
# bench/validate_epochs.py against a published batch-1 measurement.
replicas = 256
# Sequence cap and token budget both present, because which one binds is the point. At a ~3.2k mean
# context the token budget allows about 420 sequences, so the count cap binds for ordinary traffic
# and the token budget binds once long prompts arrive.
max_batch = 256
kv_capacity_tokens = 1370000
# Weight read for a 70B model on 8xH100 at 70% bandwidth utilization, 7.46 ms, plus 2.75 ms of fixed
# overhead. With the bandwidth term below this reproduces bench/validate_epochs.py to within 0.05 ms.
step_base_ms = 10.2
step_per_seq_ms = 0.0
step_per_kv_ktoken_ms = 0.0175
prefill_tokens_per_s = 28286
step_token_budget = 1024
max_queue = 400

# Workload. A mixture, not a single distribution: interactive chat plus long-document work in the
# same queue. The mixture is what creates head-of-line blocking, so a unimodal load would hide the
# phenomenon being studied.
arrival_rps = 2254
prompt_mean = 1200
prompt_cv = 1.2
output_mean = 300
output_cv = 1.5
long_probability = 0.08
long_prompt_mean = 24000
long_output_mean = 400

routing = weighted_random
wr_c1 = 1
wr_c2 = -0.3
wr_c3 = -0.5
wr_c4 = -0.05
wr_c5 = -0.1
scheduling = buffered_batch
buffer_max_hold_ms = 5
telemetry_interval_ms = 1000
telemetry_delay_ms = 200

client_timeout_s = 60
max_attempts = 1
ttft_slo_ms = 2000
itl_slo_ms = 80
e2e_slo_s = 60
sample_interval_ms = 250
