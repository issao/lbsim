# HELD-OUT SUITE 5 of 8 — frozen. docs/arena.md section 5.
#
# Dynamic exposed: a bursty step change. 50 rps for the first 45 s, 3x that for 30 s, then back. Mean
# offered load over the measured window is 79 rps against an analytic rated capacity of 328, so the
# load is in scope for scoring while spending 30 s of it at 150 rps — right at the measured SLO
# capacity from h2. At freeze time the best policy attained 0.9884 across the whole window.
#
# Why it is in the suite: two reasons that no steady scenario covers. First, recovery — whether the
# fleet drains after the burst or stays collapsed, which is the difference between a transient and an
# incident. Second, it is the load shape docs/arena.md section 3 warns about: "the load generator is
# free to change the load after warm-up", so a rated capacity declared during the quiet first phase is
# tested against the burst. The burst starts at 45 s, well after the 15 s warm-up ends, so a policy
# that tuned its declaration to what it saw during warm-up over-claims.
name = h5-bursty-step
seed = 20260906
duration_s = 120
warmup_s = 15

# -- fleet: a 70B-class model on 8xH100 per replica, identical in every held-out file ---------------
# calibration.md 6.5: batch-1 decode 10.25 ms (NIM, 8xH100 bf16) and 11,082 output tok/s at max
# throughput; 6.2's convention puts all overhead in the fixed term. step_base_ms is the 7.46 ms weight
# read at 70% bandwidth utilization plus 2.75 ms fixed overhead, step_per_kv_ktoken_ms is the
# bandwidth term, and kv_capacity_tokens is the primer's 1.37 M per replica (calibration.md 5.3 uses
# the same figure). Replica count is not a calibrated number: calibration.md 9.2 records that no public
# source gives a fleet size.
replicas = 32
max_batch = 256
kv_capacity_tokens = 1370000
step_base_ms = 10.2
step_per_seq_ms = 0.0
step_per_kv_ktoken_ms = 0.0175
prefill_tokens_per_s = 28286
step_token_budget = 1024
max_queue = 400

# -- load: the h1/h2/h3 chat mixture, with a step ------------------------------------------------
# Mixture identical to h1 (Azure 2024 conv plus a Mooncake long mode) so the burst is the only new
# thing. load_step_factor 3.0 is inside the diurnal peak/trough swings calibration.md 2.3 measures
# (8.96x for Azure code hour-of-day, 33.1x worst hour, 34.8x BurstGPT conv). The 30 s duration is NOT
# measured: calibration.md 2.4 says no source publishes a burst-duration distribution and offers
# 10-60 s inferred from IDC saturation, and 9.1 gives it a [GUESS] default of exactly 30 s with a
# 5-300 s sweep range. That guess is used here and flagged.
arrival_rps = 50
prompt_mean = 1632
prompt_cv = 0.94
output_mean = 105.5
output_cv = 1.5
long_probability = 0.08
long_prompt_mean = 12035
long_output_mean = 343
load_step_at_s = 45
load_step_factor = 3.0
load_step_until_s = 75

# -- telemetry: calibration.md 9.2 records production staleness as unmeasured, [GUESS] 5 s with a
# 1-30 s sweep. 1 s is the fast end of that sweep, so herding is present but not the subject here.
telemetry_interval_ms = 1000
telemetry_delay_ms = 200

# -- client and SLOs: identical to the chat scenarios --------------------------------------------
client_timeout_s = 60
max_attempts = 1
ttft_slo_ms = 2000
itl_slo_ms = 80
e2e_slo_s = 60
sample_interval_ms = 250
