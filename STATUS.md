# STATUS

What is live on `origin/master`, what each agent is doing now, and the assumptions being acted on.
For things that need *you*, see `TASKS.md`.

**Last updated:** 2026-09-07 19:49 PDT by Claude.

**RESUMED 2026-09-07 19:13 PDT, at Issao's "resume", relayed by main.** The pause held from 11:22 to 19:13 (Issao at 11:22, verbatim via main: *"actually, pause everything, we are almost out of quota. we will resume when we are back within limit"*); nothing landed on `master` during it except the tech lead's graph pause checkpoint (de7891a, merged 1a58186 11:26: PAUSED 11:24, a Resume paragraph for each of U79–U82). Agent ids from main at the resume: tech lead `a56ff1d3950e7375f` (Fable) resuming U79–U83; main `main`; productivity not running. Housekeeping is on `claude/docs-round39` from `origin/master` 023672d, loop re-armed. **The four wave-1 branches at the resume, read from `origin` at 19:13, and the tech lead's plan for each (main's word):** `claude/tl-home-sections` 50e4cf0 (one WIP commit, "U80 pause checkpoint"), **U80 landed** as 023672d at 19:13 (rebased to 0110319; `web/src/pages/Home.tsx` only, +43/−80; the branch deleted from `origin`): Home restructured into Live / Replay / Mock sections, every link in exactly one section. Main's word at 19:15: the commit title says WIP only because the pause instruction told every agent to commit under that title; the agent had reported the build green and every Home check passing before the pause. **Not deployed yet**: deployed is still `lbsim-00013-mvq` from c22ea56, and READY TO DEPLOY goes to main once U80–U83 are all on `master` (the tech lead at 19:17, who resumed at 19:12 and will message each hash as it lands); `claude/tl-mode-badge` at da04703 (WIP checkpoint, `mode.ts` only), which **landed as U81, bd5ba3f at 19:32** (read from git: badge per route and state in `App.tsx`, `mode.ts`, `Dashboard.tsx`, `Compare.tsx`, the `ui.tsx:59` one-liner, qa.js badge assertions; a mount-order race fixed on the way, App's reset moved to a layout effect; local gate 44 passed, the 12 failures the documented `QA_SKIP_REPORTS=1` 404s); `claude/tl-finished-text` at 192e013, `master` at the pause with no WIP commit, so U82 was re-spawned from scratch and **landed as 837910b** (build-process references out of user-facing text); `claude/tl-cloud-hang` at bae16cc (WIP checkpoint), which **landed as U79, 60327b7 at 19:33** (the caveat fix above; `server.rs` +419, WIRE.md, `run.rs`, `lib.rs`); **U83 queued** behind U81 as before. Of the five, U80, U82, U81 and U79 are on `master` as of 19:34 (U81 confirmed by the tech lead at 19:35, with its deviation: the `ui.tsx:59` grant and the mount-order race fixed; **U79 confirmed finished** at 60327b7 by the tech lead at 19:36, one deviation recorded as a small follow-up: rejected OpenSubscription opens (400/404/410) are not logged as `req` lines, only the two `sse` lines; READY TO DEPLOY 60327b7 went to main at 19:38 with the harness output, a second READY follows U83); **U77 landed** f4a119f at 19:40 (`run.rs` and WIRE.md only, 52 sim-ingress tests; confirmed by the tech lead at 19:41, no deviation: the live-run cap counts busy or leased runs only, StartRun at the cap evicts the oldest idle-stopped run before answering 503, an idle-stopped run unregisters itself after 2×IDLE_SHUTDOWN_SECONDS with its checkpoint left on disk, GetRun on an evicted run is the existing 404); not in `lbsim-00014-gvp`, rides the second deploy; **U83 spawned 19:35** (default, `claude/tl-ui-pass`, port 8195: playback bar reads play / speed / position with rewind absent where rewind is off, the "mock run" tag gone, the Run tab's duplicate playback controls gone, live and replay banners one line each with the long reasons on hover, `dropped` fields disabled in place with a server note, before/after screenshots; a sonnet reviewer over the after-screenshots once it lands, its findings becoming a unit), and U77 behind U79; graph checkpoint 0a1d41f (merged e158fb1 19:35): **70 done, 2 in flight, 12 queued, 1 waiting on Issao;** in flight now U83 only, the tech lead running the harness against `lbsim-00014-gvp` and sending the result with the second READY; **deployed 19:39 as `lbsim-00014-gvp` from c2329f7** (U79–U82), U83 and U77 to follow in a second deploy; this file records each as the tech lead or main reports it.

**RESUMED 2026-09-07 11:17 PDT.** Housekeeping is back on `claude/docs-round38` from `origin/master` 00c6021. Agent ids
from the main agent at the resume: tech lead `a0899ca58df6febdc` (Fable), running U79–U83, the Cloud Run
live-server fix and Issao's product-polish pass, recorded in `docs/execution-graph.md` as it goes; the
productivity agent is not running; main is `main`. Issao's instruction this morning, relayed verbatim by main at 11:18 and
quoted in `TASKS.md` item 4, is U80–U83: homepage links in three sections (live, replay, mock), an accurate
mode message top right on every page, every reference to the build process gone from the product text, and
a pass over the UI with no knob that does nothing; the tech lead's graph entry (5080f22, merged 37893ca 11:19) carries it verbatim, checked word for word against main's relay. The graph resumed at 5080f22 (merged 37893ca 11:19): **67 done, 4 in flight, 12 queued, 1 waiting on Issao.** Wave 1 spawned 11:19: **U79** (default, `claude/tl-cloud-hang`) the Cloud Run degradation with U77 folded in, a request log served at `GET /requests.log` and the lock order written into WIRE.md; **U80** (sonnet, `claude/tl-home-sections`) Home in three sections, Live / Replay / Mock; **U81** (sonnet, `claude/tl-mode-badge`) the top-right badge accurate per route and state, asserted by the harness; **U82** (sonnet, `claude/tl-finished-text`) build-process references out of user-facing text. **U83** (default, the UI pass) is queued behind U81, both edit `Dashboard.tsx`. No ETA in the graph. Two facts the tech lead read while briefing: the A/B page runs the mock engine on both sides, so Home lists it under Mock, not Replay (told main); and a likely cause of the Cloud Run hang, below.

**The live-server degradation of `docs/wrap-up-2026-09-06.md` §5b is fixed on lbsim.ai** (main at 19:49): three consecutive
browser-harness runs against the site at 19:40–19:55, **54 passed / 2 failed, 55 / 1, 56 / 0**, and the server's own
`/requests.log` over about 90 runs shows no "busy" and clean stream ends (final, lease expired). It is no longer a caveat;
the record is under U79 below. **Residual, not the hang:** three showcase cards were each stuck once in the first two runs,
intermittent first-sample timing; main routed it to the tech lead as a small unit after U83 and U77.

**Deployed at 19:39 as `lbsim-00014-gvp` from master c2329f7** (revision creation time from gcloud, per main at 19:40), carrying
U79 the hang fix and `/requests.log`, U80 Home sections, U81 the badge, U82 the finished-product wording; U83 and U77
ride the second deploy.

**U79, the fix, landed as 60327b7** (19:33; confirmed finished by the tech lead at 19:36: three tests of which two hung on the
unfixed code; harness 50/0 with reports; READY TO DEPLOY sent to main at 19:38). The cause was the one the graph named at
11:19: two arms of `stream_updates` called `finish_subscription` while holding the `subs` guard, and a std Mutex relocked
by its holder parks that thread forever; one parked writer then blocked every reap and every fresh OpenSubscription until
the 64-connection cap answered 503, which is why `GetRun` kept answering in 120 ms while `StartRun` hung. Now the subs
block decides a step and the writer acts after the guard drops; `drive()` reads leases before `run.state`; the order
leases → subs → run.state is written in WIRE.md; tests cover both triggers (a stop before the first closed frame, a
reconnect already at the final sequence). It also adds `GET /requests.log`, the last 512 request lines, because the
deploy identity cannot read Cloud Logging. One follow-up recorded by the tech lead: rejected OpenSubscription opens
(400/404/410) are not logged as `req` lines. The evidence and the night's ruled-out list stay in
`docs/wrap-up-2026-09-06.md` §5b; the symptom on `lbsim-00013-mvq` and earlier was that after a handful of runs
`StartRun` hung for the client's full timeout and `OpenSubscription` returned no bytes while `/health` and `GetRun`
answered.

**Final deploys of 2026-09-06, which this file missed at the stop** (revision creation times from `gcloud run
revisions list` at 11:17 today; what each carries is main's word): `lbsim-00010-spk` 21:11, the showcase-stuck
fix (7d174d0); `lbsim-00011-qs5` 21:15, U70 mock/replay/live labels (525d255); `lbsim-00012-vqs` 21:57,
U71–U76 (a5061da, 77fb48f, 8c75b9f, f97c175, de7536f, 945a727, plus reports 11 and 12 in the `Dockerfile`,
9c754c8); **`lbsim-00013-mvq` 22:04, U78 `least_kv_probe` (a73cb7c) and session affinity in `deploy.sh`
(c22ea56), current and serving 100 % with `sessionAffinity: true` on the revision.** Every code commit on
`master` is therefore on <https://lbsim.ai>; the only commits after c22ea56 are docs (the graph's STOPPED
note 4bf947d/333626c and the wrap-up §5b 00c6021). Each hash above was checked against `origin/master` with
`git merge-base --is-ancestor` this round.

**The 2026-09-06 evening, for the record.** Stopped 21:01 at Issao's request (*"we are approaching limit...
tie up loose ends... give me a doc summarizing the state and most interesting outcomes with deep links and I
will review and wrap up tomorrow"*); graph checkpoint 41e035a (21:12; 60 done, 12 queued) and the wrap-up
`docs/wrap-up-2026-09-06.md` (5da8a67, merged cc8a229 21:01). Then a second tech-lead session, 21:26–22:05
(0a0aef3), with the goal from Issao verbatim per the graph: *"ask the TL to get to the point that the load
test dashboard is running with live backend."* It spawned U71–U76 from main's 21:15 QA of lbsim.ai, then U78
from the gate's residuals, and landed all of them; its final checkpoint 4bf947d (merged 22:05) reads "STOPPED
2026-09-06 22:05 PDT: the session's scope is complete; resume from this file, U77 first": **67 done, 0 in
flight, 11 queued, 1 waiting on Issao; 12 selected dynamics, 12 showcased, 12 live, 12 showcased-live,
verified in headless Chromium on master c22ea56 (`tools/qa`, 50/50)**. Housekeeping had stopped at 21:14, so
everything from 21:15 to 22:16 above comes from `git log da01aab..00c6021` and gcloud, not from memory.
`TASKS.md` is the review list. Every hash in the live table below was checked against `origin/master` with
`git merge-base --is-ancestor` at 21:01 or this round; nothing in it is unverified.

Before the stop: resumed 20:27 PDT after a pause 17:31–20:25 at Issao's request, verbatim: *"can you
ask everyone to pause until usage limit resets in 2h 50min"*; every agent checkpointed to files before
the pause and was respawned from them at the resume.

## Session restart

**Resumed 2026-09-07 19:13 PDT.** Housekeeping restarted from this file and `TASKS.md` on `claude/docs-round39` from
`origin/master` 023672d; the predecessor paused cleanly at 11:22 (c4da40c, merged 192e013), so everything here comes
from files and `git log 192e013..023672d` (the tech lead's graph pause checkpoint, then U80's checkpoint merged at 19:13). Agent ids for this
session, from the main agent at the resume: tech lead `a56ff1d3950e7375f` (Fable, resuming U79–U83), main `main`;
productivity not running. `python3 tools/inbox.py` at 19:13: no pending marker in the tree; the six stale quote markers on
`origin/claude/tl-arena-rules`, `tl-replay` and `tl-trace-wire` are unchanged (`TASKS.md` item 3). No open PR on
`issao/lbsim`. The default window on `docs/vision-progress.md` (2026-09-07 16:19) passed during the pause; the file
is kept until Issao says, `TASKS.md` item 2.

**Resumed 2026-09-07 11:17 PDT.** Housekeeping restarted from this file and `TASKS.md` on `claude/docs-round38`
from `origin/master` 00c6021; the predecessor stopped cleanly at 21:14 on 2026-09-06 (da01aab), so everything
here comes from files, `git log da01aab..00c6021` and `gcloud run revisions list`. Agent ids for this session,
from the main agent at the resume: tech lead `a0899ca58df6febdc` (Fable, units U79–U83), main `main`;
productivity not running. `tools/sync.sh` at 11:17: protos compile, diagram consistent, secret scan clean, no
stamp ahead of the clock, epoch math exact; its one complaint is the six stale quote markers inside the old
`docs/execution-graph.md` on `origin/claude/tl-arena-rules`, `tl-replay` and `tl-trace-wire`, `TASKS.md` item 3.
No open PR on `issao/lbsim`.

**Resumed 2026-09-06 20:27 PDT.** Housekeeping restarted from this file and `TASKS.md` on `claude/docs-round37`
from `origin/master` df8c751; the predecessor was terminated by the rate limit at 17:32 (5d1aa1d), so
everything here comes from files and `git log 5d1aa1d..df8c751`. Agent ids for this session, from the
main agent at 20:25: tech lead `aa236236554a3edd1` (fresh, on Fable), productivity `a86cbbf414940cdaa`,
main `main`; both resumed 20:25–20:26 from `docs/agents/`. What landed between the housekeeping pause and
the agents' pause, 17:31–17:35, all by the tech lead and the productivity agent finishing their
checkpoints: U53 (745cdd3, merged 6f9f913 17:31: exporter covers demos 7–10, 38 runs); graph checkpoint
`tl-graph-4` (c5edb72, merged 287879c 17:33: U34, U47, U50, U51 landed; S3, U35, U40a, U40b spawned);
`prod-3` (3c48ca2, merged a3fa7a5 17:33: the 17:35 section of `docs/iteration-profile.md`, first wave
under the template, 13.8 → 5.1 min per merged unit, and brief template v3 with the sizing and whole-read
rules); graph checkpoint `tl-graph-5` (bc802a2, merged ccedaf3 17:35: PAUSED until 20:21, a Resume
paragraph for each of the nine in-flight units, U53 landed, R1's findings queued as U54 server hardening
and U55 small fixes; R1's item 8, frames cloned out of `engine.frames()`, is a crate-boundary decision
for the main agent). At the resume the main agent landed d5c8342 (merged df8c751 20:25): `tools/sync.sh`
fails on any stamp ahead of the clock, so every stamp is typed from `date`, never estimated; the grace was three minutes and is one since 29fd8d5, after the graph's 20:47 stamp (written at 20:42) slipped through at 20:45.

**Resumed 2026-09-06 17:04 PDT.** Housekeeping restarted from this file and `TASKS.md` on
`claude/docs-round36` from `origin/master` c1373d0, then 907f3a1. Agent ids for this session, from the
main agent at 17:15: tech lead `a86e5fccbc930bd58` (fresh, on Fable), productivity `a09d10732748a5738`,
cloud `a4c101059bf0331c5` (temporary, deploying the replay dashboard), main `main`. Everything that
landed between the checkpoint and the resume was recorded from `git log 35693bc..907f3a1` and the
main agent's message, not from memory. The
respawn briefs are `docs/agents/` (ad16d4d, 39ec6d7, 4b7c5d3): `README.md` gives the spawn order,
`housekeeping.md`, `tech-lead.md`, `productivity.md`, `cloud.md`, and `brief-template.md`, the preamble
every subagent spawn pastes in.

Checkpoint at 2026-09-06 16:43 PDT. Issao: *"lets commit any state that is critical for any running agent including you
and then start with fresh context from there."* Every agent was killed and restarted from files at this
point. Each agent's memory is a file, and a restarted agent resumes from it alone:

| Agent | Memory | Respawn brief |
|---|---|---|
| Tech lead | `docs/execution-graph.md` | `docs/agents/` |
| Housekeeping, monitor and inbox | `TASKS.md`, `STATUS.md`, this section | `docs/agents/` |
| Main agent | `CLAUDE.md`, `docs/agents/` | `docs/agents/` |

**Housekeeping watcher setup, to re-arm identically** (the brief in `docs/agents/housekeeping.md` is
authoritative). Work in the worktree `/home/agents/repo/lbsim-docs` on branches `claude/docs-<n>`, never
in the shared checkout; commit with `git commit -- <paths>`; merge `--no-ff` into `master` in the main
checkout after `merge --ff-only origin/master`, push `master` and the branch, verify with
`merge-base --is-ancestor`. Poll every 90 seconds: `git fetch origin`, `python3 tools/inbox.py --no-fetch`,
`gh pr list --state open`, and diff new upstream commits for lines naming Issao without a colon, which
the scanner cannot see. A background waiter exits on the first new `origin/master` commit, pending
marker or open PR, and is re-armed after each. Markers inside `docs/execution-graph.md` are the tech lead's quotes of acted-on instructions, not
new ones; act on none of them; the tech lead rephrased them at cf85752 and the tree scans clean.
Every stamp comes from `date '+%Y-%m-%d %H:%M %Z'` or the commit that carried the event. A marker in
`TASKS.md` or `STATUS.md` that asks for code is routed: verbatim under "Routed to the tech lead" in
`TASKS.md`, marker left for the actor. `docs/vision-progress.md` is deleted at the first housekeeping
round after 2026-09-07 16:19 unless Issao has said otherwise. The estimate table in
`docs/dashboard-plan.md` and every line of `docs/execution-graph.md` change only on word from the tech
lead or the main agent, never by inference. Open at checkpoint: nothing routed is unresolved except the
tech lead's units in the graph; `TASKS.md` item 0 awaits Issao's review.

## Phase

**Building, delegated.** Three long-lived agents with disjoint file ownership, recorded in `CLAUDE.md`.
Scope is `docs/scope-today.md` package B plus item 7, and both side missions Issao asked for.

## Live on `origin/master`

| What | Where | Verified by |
|---|---|---|
| Simulator, six dynamics reproducing | `src/`, `scenarios/` | `./run-demos.sh`, six HTML reports in `out/` |
| Test suite: 159 pass, 5 ignored (3 known defects, 2 slow arena reproductions run only by `tools/integrate.sh`), measured at ede7b1f before 17:26 (143 before U18); `tests/layering.rs` enforces the downward crate dependency direction | `tests/`, `crates/*/` | `tools/build.sh test --workspace` |
| Golden fingerprints: every demo, the held-out suite and a live probe, byte-identical to the baseline | `bench/golden-fingerprints.txt` | `./check-fingerprints.sh` |
| Frontend-to-Ingress wire: JSON over HTTP/1.1, SSE subscriptions, field names held to the proto by a test | `crates/sim-ingress/WIRE.md` | `cargo test -p sim-ingress` |
| Policy ordering survives ±30% cost-model error | `check-sensitivity.sh` | run it; exits non-zero on a flip |
| Arena, mechanical half, eight-scenario held-out suite | `crates/sim-arena/`, `scenarios/holdout/` | `tools/build.sh test --release -p sim-arena -- --nocapture arena_round` |
| Stand-in dashboard, three surfaces, mock data | `web/` | `cd web && npm run build` |
| **Deployed, public at <https://lbsim.ai>** since 16:16 on 2026-09-06, also <https://lbsim-irpwc2yaoa-uc.a.run.app>. **Current: revision `lbsim-00013-mvq` from `master` c22ea56, created 22:04 PDT 2026-09-06, serving 100 % with session affinity on** (gcloud at 11:17 today), carrying every code commit on `master`: the 12-of-12 showcased-and-live state verified by the browser gate, behind it `lbsim-00012-vqs` (21:57, U71–U76), `lbsim-00011-qs5` (21:15, U70) and `lbsim-00010-spk` (21:11, the showcase-stuck fix); the Cloud Run degradation after several runs is the caveat at the top of this file. Before those: **revision `lbsim-00009-qb8` from `master` 4474eb8, created 20:56 PDT** (time from `gcloud run revisions describe`), deployed by the main agent; verified in git it carries U58 (the Showcase opens a live run), U60 (demo 11 scripted), U54, U40a and U59a on top of `lbsim-00008-fbz` (c3d1b84, 20:49: U24, U35, U48, U49, U55, U56, U57), and not U40b (052d40e merged after 4474eb8, though main's record listed it); so what is on <https://lbsim.ai> at 20:57 is the graph's "10 showcased-live of 11" state, `set` steps still refused until U59b; `/runs/index.json` 200 at 20:57. Before it the 20:39 revision `lbsim-00006-7sk` (c90bf35: U22, U28, U52), whose live path was verified end to end on the public URL at 20:40 (`StartRun` with `scenarios/route_p2c.txt` at 4x realtime returned `r-1`, `GetRun` `STATE_RUNNING`, `OpenSubscription` streamed SSE updates with a 13-metric fleet row, `StopRun` finalised `STATE_COMPLETE`); `lbsim-00007-7w4` (c2bc6e1, 20:46) lasted three minutes in between. This is the deploy the tech lead calls **"9 of 11 dynamics showcased and live-runnable"**, its wording, relayed by main; the honest caveat, from main: *showcased* today means the Showcase page steps through a recording of the dynamic, *live* means the same scenario runs live on the dashboard page, and the Showcase page does not yet open a live run itself, which U58 and U59 are spawned to close. The graph's status line moved to **10 of 11** at 13d884a (20:51), by its own definition, and U58 (the Showcase opening a live run) landed at 20:50, after this deploy; the next deploy carries it. <https://lbsim.ai/#/dashboard> is the live surface; <https://lbsim.ai/runs/index.json> serves the recorded demo runs (200 at 20:51); reports at `/reports/`; the build trend and the idle note are in `docs/deploy.md` (c706671) | `Dockerfile`, `cloudbuild.yaml`, `deploy.sh` | `curl -s https://lbsim.ai/runs/index.json`; `./deploy.sh --check-idle` reads the instance count from Cloud Monitoring |
| Reference cost model, exact against a naive oracle | `bench/validate_epochs.py` | `tools/sync.sh` runs it |
| Interfaces, twelve files, reviewed; `GeneratedPolicy` slot (947649b), lease expiry renamed `lease_expires_at_wall_ns` because it is wall clock (db390a4); `TraceSpan` carries the per-resource state and `RequestTrace` its latency bucket (a035514, merged f5eddf1), awaiting Issao's review, `TASKS.md` item 0 | `proto/lbsim/v1/` | `tools/sync.sh` compiles them |
| Findings, one section per dynamic, nine now; findings 8 (the KV spiral, demo 11) and 9 (speculative decoding, demo 12 since U26b) quote the units' numbers as forwarded by the tech lead, not yet re-run tables; finding 8 confirmed by U62 (2896377) | `docs/findings.md` | every table from `./run-demos.sh` except findings 8 and 9 |
| `disable_decode` scenario key, *"basically by setting HBM to infinity"*: zeroes the bandwidth term, KV still binds in tokens; demo 7 (`route_round_robin_no_decode`, `route_p2c_no_decode`), demos 8-10 for admission, fair share and live probes; 28 golden rows added, no existing number moved | `crates/sim-physics/src/lib.rs`, `scenarios/`, `run-demos.sh` (f6b7283, merged c7f8c6a) | `./check-fingerprints.sh` |
| Policy registry generated from the policy files by `build.rs`, so parallel policy branches cannot conflict on one table | `crates/sim-policy/build.rs` (5083b7b) | `tools/build.sh test -p sim-policy` |
| `tools/integrate.sh <branch>`: the merge queue as a script any agent runs at the end of its unit (rebase, workspace test, fingerprints, `--no-ff` merge, push) | `tools/integrate.sh` (dbe5e8e) | `tools/integrate.sh --dry-run` |
| Restart briefs, one per agent, plus the brief template every subagent spawn pastes in; the goal from Issao at 16:53 recorded verbatim in the tech lead's | `docs/agents/` (ad16d4d, 39ec6d7, 5c8686c, 4b7c5d3) | read them |
| Iteration profile: a unit is 68% model time; the one 79 s arena test dominates tool time; ranked fixes; the 17:35 section measures the first wave under the brief template, 13.8 → 5.1 min wall per merged unit, first edit 4.7 → 2.0 min, and finds that the number of owned files predicts the rest, hence template v3 (≤6 owned files, `sonnet` at ≤4 files with one test, files under 400 lines read whole once); **the 20:54 section** and the 21:00 closing section, the day in numbers (b12d2e9): the resumed wave merged 13 units in 25 minutes (32 an hour, was 20), the repeated failure was rebase conflicts on the two append-only fingerprint files, 24 agent-minutes across U40a, U40b and U35, so template v4 makes those conflicts the agent's to resolve and names `./check-fingerprints.sh --update`; three proposals are with the tech lead (union merge on the golden file, `html_md5` without the scenario text, md-only diffs skipping the test gate) | `docs/iteration-profile.md`, `docs/agents/brief-template.md` (3c48ca2 merged a3fa7a5; prod-4 merged 63353d5 20:55) | read it |
| Execution graph: **STOPPED 21:12 PDT (41e035a, merged 2ac9e80): 60 done, 0 in flight, 12 queued, 1 waiting on Issao; 12 selected dynamics, 12 showcased, 10 live; no WIP branch and no worktree to resume, the queue ordered for the next tech lead.** Before it, STOPPING (7276b0c, merged f2de5aa, 21:03): 51 done, 9 in flight told to finish within ten minutes, all did; U70 labels the last unit. At 565aadf: **Three counts, per main at 20:49: of 11 selected dynamics, 11 showcased on recordings (U60), 10 live-runnable on the dashboard page (demo 11's preemption keys are not yet in the control panel's config mapping), 10 showcased-live since U58**, `set` steps refused until U59b, which landed at 20:59 (7cc3395) after this checkpoint, so the count is the tech lead's to move. Review R3: determinism holds on all six merges, U55, U56, U57 clean; three units from it, U63 (U35: unsorted trace rows underflow `t_ns -= origin`, a demo blocker; `offered_rps` is the synthetic rate in trace mode), U64 (U49 runner: `skip()` settles early on a live run, a cursor already past the step, a run ending before the step), U65 (U24 recorder: `steps` snapshots grow unbounded while any id is tracked); R3's unverified U24 items queued low as U69. Review R2's units U61 (landed) and U62. The productivity agent's measurements adopted as U66 (`.gitattributes merge=union` on the golden file, the fingerprint run list in a data file, `html_md5` without the scenario block) and U68 (`integrate.sh` skips the test gate for md-only diffs) | `docs/execution-graph.md` (41e035a, merged 2ac9e80 21:12) | read it |
| **Live Ingress server (U18)**: `POST /v1/ingress/<Rpc>` for StartRun, GetRun, ListRuns, StopRun, SetSpeed, StepForward (capped at 60 simulated s) and GetResult, one thread per run driving `Sim` so wall clock decides when to advance and never what to (a paced run has the unpaced run's fingerprint, tested); `OpenSubscription` over SSE with `event: open` then one `event: update` per sample, ids from 1, a 256-row ring for Last-Event-ID reconnect or 410, leases released on the final update; the idle guard on the run thread checkpoints a run to `runs/<run_id>/` as the export's documents, marks it `STATE_PAUSED`, and a new subscription resumes it; `/health` never touches run state (20 calls, frame count unchanged) | `crates/sim-ingress/src/{server,run}.rs`, `tests/ingress_http.rs` (82a9d63, db9999e, 1970879; merged c0e9ea8) | `tools/build.sh test -p sim-ingress --test ingress_http` |
| **Per-replica rows and the heatmap (U23)**: one `SCOPE_REPLICA` update per replica per sample in `runs/<group>/<run>/replicas.jsonl`, stamped with the fleet row's instant, KV as a fraction, step time in seconds (in `WIRE.md`'s table); the dashboard loads it when present and the machine-level heatmap shows the engine's rows on a replay, an older export without it is still complete | `crates/sim-ingress/src/export.rs`, `web/src/lib/{replay,adapter}.ts` (5e14b23, merged ede7b1f) | `tools/build.sh test --test export` and `replay.selftest.ts` |
| `tools/build.sh` polls every build slot round-robin once a second and runs cargo under `timeout` (`LBSIM_BUILD_TIMEOUT`, default 420 s), so a hung test releases its slot inside the caller's turn; `tools/build-slots.test.sh` covers it. The fix the productivity agent measured at 17:20 (three agents lost 600 s each at 16:53) | `tools/build.sh` (1e03514, merged 35c7918) | `tools/build-slots.test.sh` |
| **Arena generator loop (U34)**: render a policy file into `sim-policy`, hash it (SHA-256 written in place, no dependency), rebuild, score with the freshly built binary because the registry is generated at build time, append the catalog row; `--keep` for a manual round. The template ships three plain p2c variants until the forecasting families (U40) exist; a manual `p2c_d3` round scored 0.000 (worst h7) against p2c's 0.762, file not committed | `crates/sim-arena/` (fe71bcf, merged e304990) | `sim-run arena --keep` per its help |
| `WIRE.md` records the nine decisions the live server settled: reconnect query parameters and 410, the idle guard's definition of queued work, the checkpoint layout and its non-resumability, live versus exported `from_merged_histogram`, `METRIC_STEP_TIME` as one sample, StopRun's finalisation, lease defaults and close-on-final, 501 for unimplemented RPCs, the 1 MiB body cap with `Expect: 100-continue` | `crates/sim-ingress/WIRE.md` (f5d4350, merged 96e88d0) | read it |
| **Preemption and KV eviction (U22)**: capacity is a token budget and, until now, only a finished request gave tokens back; `recompute` drops a context and prefills it again, `swap` copies it over the host link and back, chosen per scenario with a separate victim rule (newest, largest context, most slack), every cost through `CostModel`; `never` is the default and is proven invisible (every existing golden row unchanged, `route_p2c` under `recompute` same fingerprint). Multi-turn sessions park context on the replica between turns: at two sessions a second on four replicas a 30k-token cache fills inside a minute, no eviction serves one sequence at a time (late attainment 2%, p99 TTFT 36 s), swapping to DRAM at 50 GB/s serves it at 95%, p99 TTFT 84 ms, 2.2 preemptions a second: **demo 11**, `out/11-preemption.html`. A lone running sequence is never evicted; parked context goes before a running one. The `DEMOS` table in the exporter now mirrors `run-demos.sh` and a test refuses the tree until it does (ff59e7d); 20 golden rows re-baselined because the report text gained the preemption scenario lines (586ccf4) | `crates/sim-leaf/`, `tests/preemption.rs`, `scenarios/` (d48191f, merged dcf77c8 20:34) | `tools/build.sh test --test preemption`; `./run-demos.sh` demo 11 |
| **Web app on the live transport (U28)**: the dashboard is sourced from the Ingress server when `ListRuns` answers, `ServerRunEngine` over the real client, SSE reader and subscription lifecycle, checked end to end against `fakeIngress`, an in-memory server speaking `WIRE.md`: start to first frame equal to replay's for the same row, pause and speed via `SetSpeed`, `StepForward` bounded, `Last-Event-ID` reconnect resuming at the next seq, a 501 on `UpdateWorkload` surfacing as a named reason in the "not applied" banner rather than swallowed, `StopRun` finalising `COMPLETE` on dispose; seven self-test cases | `web/src/lib/`, `web/src/pages/Dashboard.tsx`, `web/src/panels/StatusBar.tsx` (f6289ad, merged 595ea7e 20:35) | `cd web && npm run build` and the live self-test |
| **Walkthrough runner on recordings and live runs (U49)**: the showcase's step machine moved out of the React component into `walkthroughRunner.ts`, a framework-free state machine over the smallest handle that can apply a step's conditions, move to its timestamp and pause, so one machine drives the mock, a recording and a server run, and a fake handle tests the ordering (conditions before motion, pause at the timestamp, refusal without a throw); refusals are step state, a recording cannot take an override and the server answers UpdateWorkload/UpdatePolicies with 501 until those land, either way the step plays and the panel says why; Showcase opens a script's `run` through the dashboard's replay branch by pinning `?run=` before mount, scripts without a run keep the mock path, `compare` is noted, not opened (no A/B view on that page). The "N live and showcased" count is the tech lead's to move in the graph, whose 64e18b9 checkpoint predates this merge | `web/src/lib/walkthroughRunner.ts`, `web/src/pages/Showcase.tsx` (62c29bd, merged 9218eb9 20:41) | the runner's self-test; `cd web && npm run build` |
| **One vocabulary for data source everywhere: mock, replay, live (U70, the last unit of the day)**, from Issao verbatim: *"make sure that all links and pages state clearly what is mock, what is data replay and what is live."* The three banners read "word — gloss"; every panel always carries a tag, `mock` whenever it reads any unwired field or draws from a mock frame in any mode, and only a fully wired panel borrows the active mode's word (replay or live); the home page's surface cards and the reports section state their word plainly; the status bar and dashboard banners always show the mode word with its gloss; the Showcase narration header names its source. Deployed since `lbsim-00011-qs5` (21:15) | `web/src/lib/{mode,wired}.ts`, `web/src/{ui.tsx,pages/*}` (3049031, merged 525d255 21:11) | the web self-tests; `cd web && npm run build` |
| **Showcase links no longer stick on "initializing the mock run"**, from Issao at the wrap-up, verbatim in the commit: *"I am clicking on some of the showcase links and I am stuck in 'initializing the mock run'. can you look at it and fix?"* Reproduced by the main agent in headless Chromium against lbsim.ai: the client's subscription wish list included `METRIC_PREEMPTIONS_PER_S`, which the server does not serve for the fleet scope, the open answered 200 with a `rejected_reason`, and the page returned its placeholder before the banner whenever no sample had arrived; a refused metric now surfaces instead of waiting forever. Deployed since `lbsim-00010-spk` (21:11) | `web/src/` (3e78b9a, merged 7d174d0 21:09) | headless Chromium against the deployed site, per the commit |
| Golden file merges by union, `html_md5` stable across scenario-key changes (U66, the productivity agent's fix-once item): `.gitattributes` marks `bench/golden-fingerprints.txt` and the new `bench/fingerprint-runs.txt` `merge=union`, safe because `integrate.sh` stage 4 re-verifies every row after the merge; the fifteen `run` lines move out of `check-fingerprints.sh` into `bench/fingerprint-runs.txt`, keyed by label, appended never renumbered; `sim-report` wraps the verbatim scenario dump in `<!-- scenario -->` comments and the fingerprint script hashes the report with that block deleted, so a new `Scenario` field no longer moves every `html_md5` row (six stage-2 refusals cost 24 agent-minutes in one wave) | `.gitattributes`, `bench/fingerprint-runs.txt`, `check-fingerprints.sh`, `crates/sim-report/` (03133b9, 8c064e2, merged 5150654 21:10) | `./check-fingerprints.sh` |
| **Replica failures, silent and announced (U31a)**, `VISION.md` §4: a scenario carries a failure schedule; a slowdown or a hang is never announced, telemetry keeps reporting the replica and only its step time or the client timeouts betray it; a crash is announced through the same delayed telemetry as everything else, so for one delay the router keeps sending to a machine that is gone; no schedule means every existing run is byte-identical, pinned in `tests/failures`; the timeout arm's retry logic moved into `Sim::abort` so a crash, a hang and a refused dispatch retry through one path. Measured, four replicas at 30% load, one at 0.3x from t = 60: p99 TTFT 2.41 → 4.30 s, timeouts 24 → 38, and `least_requests` still sends it 15.8%. A finding-shaped result, into `docs/findings.md` when the tech lead forwards the paragraphs; the report text gained the failures line so `html_md5` rows moved (c7b33f4) | `crates/sim-leaf/`, `tests/failures.rs`, `scenarios/` (e755f67, c7b33f4, merged 33dfd18 21:06) | `tools/build.sh test --test failures`; `./check-fingerprints.sh` |
| **Demo 12, speculative decoding (U26b)**: `run-demos.sh` block 12 (`spec_off` against `spec_n4`), the exporter's `DEMOS` table gains `12-spec-decode` (42 exported runs now, was 40), the `spec-decode.json` walkthrough quotes finding 9 verbatim and the index card for dynamic 15 gets its script, so the landed cut item joins the selected dynamics as the twelfth | `run-demos.sh`, `crates/sim-ingress/src/export.rs`, `web/src/walkthroughs/spec-decode.json` (b827d84, merged 02e5dcb 21:05) | `./run-demos.sh` demo 12; the walkthrough self-test |
| Walkthrough runner fixes from R3 (U64): `skip()` on a live run no longer settles short, and `tick()` explains an overshoot (a cursor already past the step) or a run ending before the step instead of settling silently | `web/src/lib/walkthroughRunner.ts` (c2aff5d, merged 2f89623 21:05) | its self-test |
| Trace workload fixes from R3 (U63): trace rows are stably sorted by `t_ns` before the origin is taken, so a row earlier than the first no longer underflows `t_ns -= origin` (a debug panic, a release wrap that ended arrivals after one row, a demo blocker); `Frame.offered_rps` is the trace's real rate in trace mode, not the synthetic one | `crates/sim-workload/`, `crates/sim-leaf/` (86d5e55, merged f73f95f 21:03) | its tests |
| Trace recorder bounded (U65, from R3 on U24): `steps` grew one `ResourceSnapshot` per step for as long as any id was tracked, about 13 MB per replica-hour on a busy replica; `take` now drops snapshots below the smallest step index any remaining event points at and rebases the survivors | `crates/sim-model/` (44f19f5, merged 9938595 21:04) | its tests |
| **SLO classes in the engine (U25a)**: a batch job is not late because its tokens arrive unevenly and an agent loop tolerates a slower first token than a person typing, so `slo_classes` gives each arrival a class from its own random stream (every classes-off run byte-identical) and judges the finished request against its class's fixed targets from U42 (interactive 2 s / 80 ms, agent 5 s / 150 ms, batch 60 s / no ITL); per-class goodput and attainment on `RunResult`, nothing in the report yet, that is U25b; the `html_md5` rows refreshed for the `slo_classes` scenario line and the spec-decoding fixture gained the class field | `crates/sim-core/`, `crates/sim-leaf/`, `crates/sim-metrics/` (5a4f185, a835897, 65c0dee, merged b852dba 21:01) | its tests; `./check-fingerprints.sh` |
| `tools/integrate.sh` skips the test and fingerprint stages for a docs-only rebased diff (U68, the productivity agent's fix-once item): every md-only graph update ran the full gate, 57–92 s of the tech lead's turn plus 59–68 s of lock waits for the code units behind it; a diff whose every path ends in `.md` cannot move a fingerprint or fail a Rust test, anything mixed or empty still runs the full gate; `tools/integrate-md.test.sh` checks `docs_only` in isolation | `tools/integrate.sh`, `tools/integrate-md.test.sh` (02a002c, merged c97c509 21:02) | `tools/integrate-md.test.sh` |
| Session turns through admission, eviction spares the queue head (U62, from review R2 of U22): a follow-up turn arrives at the gateway as `Ev::SessionTurn` and goes through the same admission step as any request, routing pinned to the replica holding its context, a shed turn releasing the parked context, and it now counts in `first_attempts`, the retry budget's denominator; decode-growth eviction exempts the queue head as admission-time eviction already did, so a parked context is no longer swapped out and straight back. Demo 11's numbers held (0.953, 84 ms, 2.24/s), both spiral runs' step fingerprints unchanged, only two summary rows moved; finding 8 is no longer provisional | `crates/sim-leaf/` (51968a7, merged 2896377 21:00) | `tools/build.sh test --test preemption`; `./check-fingerprints.sh` |
| **Live updates (U59b)**: `UpdateWorkload` and `UpdatePolicies` apply on the run thread, forward-only, through `Sim::apply_overrides` (U59a), with key policing by kind, so a live `set` step in a walkthrough now changes the run instead of being refused with a 501; the last unit between the graph's three counts and one | `crates/sim-ingress/src/{server,run}.rs` (11165ba, merged 7cc3395 20:59) | `tools/build.sh test -p sim-ingress --test ingress_http` |
| **Speculative decoding (U26)**, `VISION.md` §3a's value-and-cost question: scenario keys `spec_draft_tokens` and `spec_accept_rate`, off by default; the gain is a deterministic per-sequence fractional accumulator of the expected (1 − a^(N+1))/(1 − a) tokens a step, not a random draw, so every existing run is byte-identical and the `route_p2c` golden row proves it (a per-step geometric draw from a named stream is the later refinement); the cost is verify compute, decoding × N / prefill tokens/s added to the step, the bandwidth term untouched because the weights are read once a step either way; `rated_rps` carries both sides. The crossover where verify compute equals the fixed step cost is 72 sequences at the defaults: 2.6x tokens/s at batch 4, 1.8x at 36, 0.9x at 144. Report text gained the speculation lines, so `html_md5` rows moved (9f7039a). Finding 9 in `docs/findings.md`, from the paragraph the tech lead forwarded at 20:59 | `crates/sim-physics/`, `crates/sim-leaf/`, `scenarios/` (6fd648e, 9f7039a, merged 0039c28 20:57) | its tests; `./check-fingerprints.sh` |
| `useServerRun` fixes from review R2 (U61): `restart()` checks `disposed` and a generation counter after the awaited `StopRun`, so a restart no longer revives a disposed engine; a refused or 501 update restores the previous config and bumps the revision, so the refused value no longer sits in the control panel; two self-test cases | `web/src/lib/useServerRun.ts` (f31b45f, merged 4f7ad62 20:56) | its self-test |
| **`forecast_latency` routing policy (U40b)**, the second forecasting family from 16:22: `p2c`'s sampling, byte-identical candidate sets proven by feeding cloned seeded rngs through both `choose()`s and comparing their next draw, then route to the minimum predicted TTFT for this request (queued prefill ahead plus its own, over the prefill rate, plus one step); at 0.8 of rated load with `long_probability = 0.16` TTFT p99 3,768 → 3,291 ms and attainment 0.6235 → 0.6349 against p2c, identical at 0.3; a plain `run` line in `check-fingerprints.sh` (13-forecast-latency, renumbered after U40a took 12) because `compare` refuses scenarios whose workload differs; two golden rows added, none touched | `crates/sim-policy/src/forecast_latency.rs`, `tests/policy_forecast_latency.rs` (9cae0fa, eb176f3, merged 052d40e 20:54) | `tools/build.sh test --test policy_forecast_latency`; `./check-fingerprints.sh` |
| **A running `Sim` takes workload and policy overrides, forward only (U59a)**: `Scenario::with_override` is `override_key`'s text round trip moved to the crate that knows the key names, `Scenario::override_kind` says which keys a running engine can take (workload keys hold from the next arrival because the workload reads the scenario on every draw; policy keys rebuild the router and admission policy with fresh, empty state; everything else needs a restart), and `Sim::apply_overrides` checks the whole batch before anything moves, so a refused key leaves the run byte-identical to a control; the server arms are U59b | `crates/sim-core/`, `crates/sim-leaf/` (7470b54, merged 60d1421 20:52) | its tests |
| **Showcase opens a live run (U58)**: the dashboard takes its data-source decision as props (`run` id for the replay branch, `data="server"` to start the scenario live without re-probing, beside `onRun`), so the Showcase decides once per script (a switched-on server takes every script live, a named recording plays when the index serves it, the mock otherwise) and the URL is no longer rewritten; a live `set` step goes through the server's update and a 501 shows as the step's reason; `.banner.rejected` gets the refusal colour U57 owed | `web/src/pages/{Dashboard,Showcase}.tsx` (9bfe832, merged a94693b 20:50) | the walkthrough self-tests; `cd web && npm run build` |
| Demo 11 scripted as the eleventh showcased dynamic (U60): `kv-spiral.json` carries `11-preemption/kv-spiral-never` and `kv-spiral-swap` and quotes finding 8's numbers in its steps, plus goodput 1,426 against 780 tok/s from the demo report; the two run ids in the self-test's allow-list | `web/src/walkthroughs/kv-spiral.json` (c16675b, merged 2ce802c 20:51) | the walkthrough self-test |
| **Server hardening from review R1 (U54)**: a hostile or careless client no longer takes the server, or every run in it, down with it. The JSON parser carries a depth and refuses past 64 (a body of open brackets used to recurse off the thread stack and abort the process); SSE writes time out at 30 s (a peer that stopped reading held a thread, a connection slot and a lease forever); the ninth live run is a 503 (no ceiling before, unpaced runs never reaped); the idle checkpoint is built under the run lock and written after it; the last frame can no longer go out unfinal and then final again at natural end, the stop request ends the stream; a failed `advance_to` keeps the frames it had closed. The ring-and-lease leak on a write error is fixed by reaping expired leases on every ingress request, not by dropping the subscription on the spot as the review asked, because `Last-Event-ID` reconnect within the lease is a `WIRE.md` promise and a test. `WIRE.md` gains the Cloud Run paragraph: a paced run stands still without a subscriber, and that is the idle rule (23ab703) | `crates/sim-ingress/src/{server,run,lib}.rs`, `tests/ingress_http.rs`, `WIRE.md` (19a7de8, 23ab703, merged 5ba19ab 20:49) | `tools/build.sh test -p sim-ingress --test ingress_http` |
| **`forecast_load` routing policy (U40a)**, the first of the two forecasting families Issao asked for at 16:22: power of two choices over a *predicted* queue depth, the slope fitted between the last two distinct views per replica and extrapolated over the telemetry delay, the catalog's `queue_depth_extrapolation`; candidates drawn in exactly `p2c`'s order so both policies see byte-identical candidate sets at a seed and any difference is in the scoring (tested at `p2c_choices=1`, where a fingerprint mismatch would mean a stray draw); beats p2c under stale telemetry in the direction asserted; 12-forecast-load golden rows added, none changed | `crates/sim-policy/src/forecast_load.rs`, `tests/policy_forecast_load.rs` (9b4fc5c, e4814a2, merged 9ee9841 20:48) | `tools/build.sh test --test policy_forecast_load`; `./check-fingerprints.sh` |
| **Trace replay workload (U35, M4)**: `workload = trace` with `trace_file`, every arrival time, shape and tenant taken from the CSV row and no random stream touched, so replay is exact and the synthetic path's fingerprints prove it untouched; the trace is anchored on its first row so a window cut from a longer capture replays without editing timestamps; `scenarios/traces/sample.csv` (195 rows, a burst at 12–14 s) and `scenarios/trace_replay.txt`; only `html_md5` moved | `crates/sim-workload/`, `scenarios/traces/` (ad4ff74, merged 8092332 20:43) | its two tests; `sim-run` on `scenarios/trace_replay.txt` |
| `Sim::drain_frames` (U56, from R1 item 8 as the main agent decided it): the run thread used to clone each new frame out of `engine.frames()` while the engine kept its own copy, so a live run held every frame twice, unbounded; `drain_frames` hands ownership to the caller, the drive loop drains into the run's frames instead of diffing by length, `into_result` reports only the undrained remainder (the smaller of the two ways, the agent reports) and the Finish branch reattaches the frames once for `GetResult`; `frames()` and the export/fingerprint path untouched, a caller that never drains gets the same byte-identical `RunResult` | `crates/sim-leaf/`, `crates/sim-ingress/src/run.rs` (f29d7f4, merged 720db0e 20:42) | `draining_yields_the_same_frames_as_not_draining` |
| `UpdateBanner` reports a rejection as a rejection (U57): the banner never checked `accepted`, so a refused update (replay refusing a physics change, or a live 501) rendered as `required_resimulation = false ... nothing was re-simulated`, which reads as success; the wording is now a pure `updateBannerText` that checks `accepted` first and returns the reason verbatim, ahead of the re-simulation branch; the `rejected` class has no distinct colour yet because `styles.css` was not the unit's | `web/src/panels/` (b8c2a17, merged 8475ed3 20:42) | its self-test |
| Small fixes from R1 and the cloud agent (U55): the live server emits replica `METRIC_STEP_TIME` as a seconds value like `export.rs`, so the live heatmap's step time is no longer NaN, `WIRE.md` decision 5 reworded; the `kv_capacity_tokens` `.max(1.0)` guard in the export; `jsonl` served as `text/plain; charset=utf-8` | `crates/sim-ingress/src/{run,export,lib}.rs`, `WIRE.md` (a0976fd, merged e41ec81 20:41) | `tools/build.sh test -p sim-ingress` |
| Showcase scripts for the ten selected dynamics (U48): the walkthrough scripts' `run`/`compare` ids match the exporter's (`7-no-decode/*`, `8-admission/*`, `9-fair-share/*`, `10-probes/*` confirmed), every one of the ten under the same strict self-test check, and the self-test runs under plain node (`import.meta.env` read lazily, dynamic imports as `api.selftest.ts` does) | `web/src/lib/walkthrough.ts`, `walkthrough.selftest.ts` (276944f, merged a98d618 20:38) | `cd web && npm run build` and the walkthrough self-test |
| Mode-aware mock tags (U52): a panel drops its `mock` tag when everything it reads is wired, and `replicas[].state` is kept out of the wired set because `docs/dashboard-plan.md` §3 still lists it as mock (the announced health state is compared against true speed), locked in by a self-test case | `web/src/lib/wired.ts` (e44612a, merged c90bf35 20:36) | `web/src/lib/wired.selftest.ts` |
| Exporter covers demos 7–10 (U53): `7-no-decode`, `8-admission`, `9-fair-share`, `10-probes` in `sim-run export --demos`, guarded by a test that parses `run-demos.sh`; 38 runs, 176 MB, 2.1 s | `crates/sim-ingress/src/export.rs` (745cdd3, merged 6f9f913) | `tools/build.sh test --test export` |
| Two fix-once items from the iteration profile: `tools/api-card.sh <crate> [pattern]` prints a crate's public items with `file:line`, so briefs carry excerpts instead of whole files (3b82172, merged 128fc26); the last exhaustive `Scenario` literal in a test replaced by `default()` plus field assignment, so a new key breaks no other branch (e2a472f, merged 072aa03) | `tools/api-card.sh`, `tests/trace_wire.rs` | `tools/api-card.test.sh` |
| **Dashboard replay source**: with `runs/index.json` served beside the app the load-test dashboard plays exported runs, load, throughput, latency, imbalance and KV real, the rest NaN or still mock and tagged; play/pause/speed/step/scrub local, rewind and policy changes refused with a reason; 15+33 self-test cases; headless Chromium showed the 30 demo runs replaying | `web/src/lib/{replay,adapter,mode}.ts`, `web/README.md` "Replay mode" (4fb1105, merged 5077a34) | `cd web && node --experimental-strip-types src/lib/replay.selftest.ts` |
| **Trace engine (U24)**: real spans for a seeded sample of requests; sampling decided at arrival from a new seeded stream `trace`, so the run is byte-identical with tracing on or off and at rate zero the stream is never read; the replica records admission, prefill chunk, snapshot, decode step and retirement through sim-model's `Tracer` (typed on sim-core only), the loop adds gateway, routing and replica-queue spans, U19's stratified sampler decides what is retained; `RunResult` carries the traces and `sim-run export` writes them beside the run; the last prefill chunk and the first decode step are deliberately the same step. `trace_sample_rate = 0` now appears in `Scenario::to_text`, so every `html_md5` moved while fingerprint, events and `summary_md5` stayed byte-identical (48981f4) | `crates/sim-model/`, `crates/sim-leaf/`, `tests/trace_engine.rs` (a3b5141, merged 3a350b1 20:40) | `tools/build.sh test --test trace_engine` |
| Request traces on the wire: `RequestTrace`/`TraceSpan` per `WIRE.md`, a seeded stratified sampler, `GetTraces` filters, `runs/<id>/traces.jsonl` in the export inside a 5 MiB budget with every failure kept and the slowest request anchored; fed by the engine since U24 | `crates/sim-metrics/src/trace.rs`, `crates/sim-ingress/src/trace_wire.rs`, `tests/trace_wire.rs` (c766e2f, merged 3a00f92) | `tools/build.sh test --test trace_wire` |
| Arena rule set v2: the score is the minimum over in-scope loads of goodput as a share of offered output tokens, cap 0.95, recorded in every score; absolute goodput the diagnostic; `catalog::append` writes a row per arena-authored policy into `docs/policy-catalog.md`; the two 80 s arena reproductions `#[ignore]`d behind a smoke round, 81.7 s → under 2 s | `crates/sim-arena/src/{lib,catalog}.rs`, `tests/arena_{rules,catalog}.rs` (14aa4e5, merged f6a87a9) | `tools/build.sh run --release --bin sim-run -- arena --cap 0.95` prints the rule set and p2c 0.762 > round_robin 0.732 > random 0.705 |
| Policy catalog, every policy idea so far, one table per family, including the load and latency forecasting families Issao asked for at 16:22; the arena generator appends a row per policy it authors | `docs/policy-catalog.md` (fc06095; v2 scores since this round) | `tools/build.sh test --test arena_catalog` holds its headers to the code |


Eight dynamics now: the six below; since c7f8c6a, that the routing ordering holds with decode
disabled, p2c 97.0% attainment against round robin 93.7% at identical throughput, which is what
`VISION.md` §3a predicted: the rolling hotspot does not need LLM physics, finding 7 in `docs/findings.md`; and since dcf77c8 (20:34) the
KV spiral, demo 11: parked session context fills the cache and swapping it out cures it, 2% to 95% late
attainment at the same load. Finding 8 in `docs/findings.md`, written from the paragraphs the tech lead forwarded at 20:38; its
numbers are the unit's own, not yet from a `run-demos.sh` table.

The six dynamics, one line each, all measured at 30% of rated capacity unless the sweep is over load:
reading the whole fleet routes worse than sampling two of it; herding has a staleness threshold, not a
gradient; prefill and decode contend and no chunk size wins both; past the knee more offered load
delivers less; a load-balancer metric improves while service collapses; a retry budget is the
difference between a bad minute and an outage. Numbers in `docs/findings.md`.

## What each agent is doing now

**The file to monitor for execution progress is `docs/execution-graph.md`** (cd9af2a): the tech lead's
dependency graph of every unit, done, in flight, queued or waiting on you, with a section per unit,
updated on every spawn, merge and ETA change. This file summarises; that one is the source.

`docs/iteration-profile.md` (2037a11) is the measured profile of how the tech lead and its subagents
spend a unit of work, with ranked fixes; its numbers live there, not here.

| Agent | Owns | Now |
|---|---|---|
| Tech lead `a56ff1d3950e7375f` | `src/`, `tests/`, `scenarios/`, `web/src/lib/` | **2026-09-07 19:13 PDT, per main at the second resume of the day: `a56ff1d3950e7375f` (Fable) resumes U79–U83 from the graph's Resume paragraphs (de7891a). The four wave-1 branches on `origin` at 19:13: **U80 landed** 023672d at 19:13 (from 50e4cf0, rebased 0110319; `Home.tsx` only; Home in Live / Replay / Mock sections, every link in one section; finished before the pause, WIP in the title only by the pause instruction, per main at 19:15; not deployed until READY for the UI wave); **U81 landed** bd5ba3f 19:32 (badge per route and state, `none` on every hash change, brand line "inference fleet simulator"; mount-order race fixed with a layout effect; `ui.tsx:59` granted; confirmed by the tech lead); **U82 landed** 837910b (re-spawned from `claude/tl-finished-text` at 192e013, no WIP; per the tech lead at 19:25: `index.html` title now "lbsim", Showcase intro and card foot "coming", StatusBar live line, walkthroughs `index.json` note and `requires` fields, `least-kv-probe.json` and `retry-storm.json` wording; no build words in `out/*.html`; no deviation from the brief); **U79 landed** 60327b7 19:33 (the Cloud Run hang: finish outside the subs lock, lock order in WIRE.md, `GET /requests.log`; confirmed finished by the tech lead; READY sent to main 19:38; **deployed 19:39 as `lbsim-00014-gvp` from c2329f7** with U80–U82); **U77 landed** f4a119f 19:40 (`run.rs`, WIRE.md; cap counts busy or leased only, oldest idle-stopped evicted at the cap, self-reap at 2× the idle threshold; confirmed, no deviation; not yet deployed); U83 the only unit in flight; **U83 spawned 19:35** (`claude/tl-ui-pass`); U83 queued behind U81. **U77** (idle-stopped runs and the live-run cap) is a separate unit after U79 from 19:17, no longer folded in, because both edit `drive()`'s idle block and `Server::reap`, per the tech lead. **Graph resume checkpoint c4a0cdc (merged 4739a17 19:18): 68 done, 3 in flight, 13 queued, 1 waiting on Issao;** READY TO DEPLOY goes to main once U80–U83 are on `master`, and again when U79 lands.** Before the pause, 11:19–11:22, per main: running U79–U83, the Cloud Run live-server fix (the caveat at the top; U79 then U77) and Issao's product-polish pass, recorded in `docs/execution-graph.md` as it goes; the instruction is quoted in `TASKS.md` item 4.** The graph resumed at 5080f22 (merged 37893ca 11:19): **67 done, 4 in flight, 12 queued, 1 waiting on Issao.** Wave 1 spawned 11:19: **U79** (default, `claude/tl-cloud-hang`) the Cloud Run degradation with U77 folded in, a request log served at `GET /requests.log` and the lock order written into WIRE.md; **U80** (sonnet, `claude/tl-home-sections`) Home in three sections, Live / Replay / Mock; **U81** (sonnet, `claude/tl-mode-badge`) the top-right badge accurate per route and state, asserted by the harness; **U82** (sonnet, `claude/tl-finished-text`) build-process references out of user-facing text. **U83** (default, the UI pass) is queued behind U81, both edit `Dashboard.tsx`. No ETA in the graph. Two facts the tech lead read while briefing: the A/B page runs the mock engine on both sides, so Home lists it under Mock, not Replay (told main); and a likely cause of the Cloud Run hang, below. The evening session of 2026-09-06 (21:26–22:05, its id not relayed to this file) landed U74 paced start (a5061da 21:31), U72 extra keys (77fb48f 21:35), U73 the browser gate (8c75b9f 21:35), U71 (f97c175 21:36), U75 showcase URL (de7536f 21:39), U76 pending controls (945a727 21:49), U78 `least_kv_probe` (a73cb7c 22:01), and the graph's STOPPED checkpoint 4bf947d (22:05): 67 done, 0 in flight, 11 queued, U77 first. Before that, **resumed 20:25 as `aa236236554a3edd1` from `docs/execution-graph.md` (bc802a2), on Fable**, following the graph's resume protocol: re-spawn the nine in-flight units from their Resume paragraphs, U22 and U24 first (one-file fixes), then U28 (critical path), nothing new until the pipeline is back at eight. Before the pause, restarted 17:07 as `a86e5fccbc930bd58`. **Landed since the resume, read from git at 20:36:** U22 preemption and KV eviction with demo 11 (d48191f, merged dcf77c8 20:34) U28 the web app on the live transport (f6289ad, merged 595ea7e 20:35), the two units the graph's resume protocol put first, and U52 mode-aware mock tags (e44612a, merged c90bf35 20:36); graph checkpoint tl-graph-6 (cf30e7f, merged 9c33433 20:35): 33 done, 10 in flight, 14 queued, S3 paused; the tech lead's message at 20:38 confirms all three and forwarded finding 8's paragraphs, now in `docs/findings.md`. U28 is not yet checked in a browser against the real server; U49 does that. U48 showcase scripts landed at 20:38 (276944f, merged a98d618), so U49, the runner, is the last unit before the first "N live and showcased" number moves; U24 trace engine landed at 20:40 (a3b5141, merged 3a350b1: 21 `html_md5` rows refreshed, no fingerprint moved), the second of the two gate-refused units from the pause; U55 (e41ec81) and **U49 the walkthrough runner (9218eb9) at 20:41**; U56 `drain_frames` (720db0e) and U57 the update banner (8475ed3) at 20:42; U35 trace replay (8092332) at 20:43; U40a `forecast_load` (9ee9841) at 20:48; U54 server hardening (5ba19ab) at 20:49; U58 Showcase live (a94693b) at 20:50; U60 demo 11 scripted (2ce802c) at 20:51; U40b `forecast_latency` (052d40e) at 20:54; U26 speculative decoding (0039c28) at 20:57; U70 mock/replay/live labels (525d255) at 21:11, the last unit; U66 golden union (5150654) at 21:10; the showcase-stuck fix from Issao's report (7d174d0) at 21:09; U31a replica failures (33dfd18) at 21:06; U26b demo 12 (02e5dcb) and U64 runner fixes (2f89623) at 21:05; U63 trace fixes (f73f95f) at 21:03; U65 recorder bounded (9938595) at 21:04; U25a SLO classes (b852dba) at 21:01; U68 md-only gate skip (c97c509) at 21:02; U62 session admission (2896377) at 21:00, finding 8 confirmed; U59b live updates (7cc3395) at 20:59; U61 server-run fixes (4f7ad62) at 20:56; U59a overrides (60d1421) at 20:52; graph STOPPED at 41e035a (merged 2ac9e80 21:12). Cloud Run CPU throttling is documented, not changed: a paced run without a subscriber stands still by `WIRE.md`'s idle rule, U54 adds the paragraph. `.gitignore` narrowed at 20:42 on the tech lead's request via main: `*.csv` ignored U35's `scenarios/traces/sample.csv`, now only the telemetry dumps are ignored. Landed since the restart: U18 live Ingress server (c0e9ea8, the previous session's agent finished it; 38 unit and 8 HTTP tests, fingerprints PASS; Rewind, UpdateWorkload, UpdatePolicies and GetTraces answer 501 until their units land), U23 per-replica rows and heatmap (ede7b1f), U45 `tools/build.sh` slots and timeout (35c7918), U46 (e2a472f), U34 arena generator loop (fe71bcf), U47 `tools/api-card.sh` (3b82172), U50 `WIRE.md` decisions (f5d4350); U53 (6f9f913), graph at bc802a2 (17:35): 31 done, nine in flight, 17 queued; R1 done, findings queued as U54 and U55. In flight: U28 web on the live transport, U22 preemption and KV eviction, U24 trace engine on the default model; U48 showcase scripts for the ten selected dynamics, U51 the `TraceSpan` fields from a035514, U52 mode-aware mock tags (a panel drops its tag when everything it reads is wired, ETA 18:15), U53 the exporter covering demos 7-10 with a test that parses `run-demos.sh` (ETA 18:10), on `model: sonnet`. Queued behind U28 and U48: U49, the walkthrough runner, *"the unit that turns 'N live and showcased' from 0 to 10."* Finding 8 (U22) is in `docs/findings.md`. Twelve finished worktrees removed. The stale `claude/tl-*` remote branches from the previous session are content-merged but the tech lead could not delete them; `TASKS.md` "Later" has the one-liner for Issao |
| Productivity `a86cbbf414940cdaa` | `docs/iteration-profile.md`, `docs/agents/brief-template.md` | **Not running on 2026-09-07, per main at the resume.** Before: resumed 17:05 from `docs/agents/productivity.md`, per Issao: *"farming out a separate agent to focus on instrospecting on overall productivity improvements for the tl and coding agents."* Works in `/home/agents/repo/lbsim-prod` on `claude/prod-<n>`, integrates with `tools/integrate.sh`. Every 20 minutes it parses the subagent transcripts and appends a dated section to the profile; first section, the 17:12 baseline, integrating from `claude/prod-1` at 17:15: three subagents of the old tech lead each lost 10 minutes at 16:53 to a hung `tests/ingress_http.rs` that held a build slot; a `tools/build.sh` proposal went to the tech lead. Second section, 17:35 (3c48ca2, merged a3fa7a5): the first wave under the template, seven units merged at a mean 5.1 min wall against 13.8 before; template v3 with the sizing rule. Resumed 20:26 from `docs/agents/productivity.md`; before the pause it was `a09d10732748a5738`. Third section, 20:54 (merged 63353d5 20:55, reported to housekeeping at 20:56): the resumed wave, 13 merged in 25 minutes, the golden file the new bottleneck, template v4; three proposals with the tech lead. Closing section, 21:00, "the day in numbers" (4ce5e77, merged b12d2e9 21:01, verified); **stopped 21:14, watcher and timer stopped; previously 21:01**, its cycle monitor stopped, worktree `/home/agents/repo/lbsim-prod` clean on `claude/prod-5`. Nothing needs Issao |
| Cloud `a4c101059bf0331c5`, temporary; redeploys are run by the main agent | `Dockerfile`, `deploy.sh`, `cloudbuild.yaml`, `docs/deploy.md` | **Not running on 2026-09-07.** Main's last four deploys of 2026-09-06, times from gcloud: `lbsim-00010-spk` 21:11 (showcase-stuck fix), `lbsim-00011-qs5` 21:15 (U70), `lbsim-00012-vqs` 21:57 (U71–U76, reports 11 and 12 in the `Dockerfile` 9c754c8), **`lbsim-00013-mvq` 22:04 (U78, session affinity c22ea56), current.** Before those, **four revisions by the main agent that evening: `lbsim-00006-7sk` (c90bf35) 20:39, live path verified end to end on the public URL at 20:40; `lbsim-00007-7w4` (c2bc6e1) 20:46, three minutes; `lbsim-00008-fbz` (c3d1b84) 20:49, the walkthrough runner; `lbsim-00009-qb8` (4474eb8) 20:56, current, the Showcase opening live runs.** Before that the cloud agent deployed the replay dashboard as revision `lbsim-00005-dzk` at 17:14, verified 17:20, and finished again; handover `docs/deploy.md` c706671. The `Dockerfile` builds reports 7-10 and runs `sim-run export --demos` into the image so `/runs/index.json` sits beside the app (efea414, merged 6897543). Idle reading: one instance held through 17:28 because external requests, verification curls included, kept restarting the idle clock; not a failure, about a cent an hour while it is being looked at; re-check on a quiet hour. Before that the cloud agent had **finished**; redeploys are run on request: `./deploy.sh`, about 2.5 minutes end to end. First deploy at 15:22 (72dfb16); scale-to-zero verified twice (993c03a); `docs/deploy.md` is its handover. No further grant was needed, the pending `legacyBucketReader` request is withdrawn: the blocker was two bucket permissions, worked around in `cloudbuild.yaml` |
| Monitor and housekeeping | `TASKS.md`, `STATUS.md`, `README.md`, `docs/*.md` except the tech lead's and cloud's | **resumed 2026-09-07 11:17 PDT on `claude/docs-round38`**, the 90-second fetch, inbox and PR loop re-armed this round; this round recorded the final four deploys, the §5b caveat and the routed instruction. Before that stopped 21:14 on 2026-09-06 after the 20:27 resume on `claude/docs-round37`; inbox and PR loop every 90 s; this round recorded the 17:31–17:35 checkpoints and the new agent ids; no open PR on `issao/lbsim`; no pending marker in the tree (the six `tools/sync.sh` lists live in stale `claude/tl-*` branches inside `docs/execution-graph.md`, the tech lead's quotes) |

The main agent coordinates and owns `CLAUDE.md` and `proto/`.

## Progress against the execution plan

One row per milestone in `docs/execution-plan.md` §1, read from `master` at 604bb95 and updated as
units land. `docs/vision-progress.md` is the same reading by `VISION.md` requirement, taken at 16:19,
temporary, for Issao to review.

| Milestone | State | What exists, what does not |
|---|---|---|
| M0 workspace and CI | done, one gap | the section 10.8 workspace, eleven crates (163995c), `tests/layering.rs` proves `sim-ingress` cannot reach `sim-model` or `sim-physics`; no `prost`/`tonic` codegen |
| M0.5 stand-in dashboard | done | `web/`, mock data, marked as such |
| M1 walking skeleton | done and exceeded | six dynamics, `docs/findings.md` |
| M2 physics | done to the plan's list | the analytic epoch advance in Rust, integer arithmetic, with the naive per-step oracle beside it and equality rather than tolerance: 10,000 random epochs agree exactly, 77x fewer evaluations on the test seed (dbfc553); `disable_decode` (c7f8c6a); preemption with swap or recompute and KV eviction landed as U22 (dcf77c8, 20:34), opening the dynamics fan-out (U25, U26, U31 queued behind it) |
| M3 three-layer split | seam done, split not | `sim-leaf-api` defines `Leaf` message for message after `leaf.proto`, `LocalLeaf` implements it in-process over a resumable `Sim`, and a Leaf binary exists in the manifest (30e6ef1); the process split Issao decided on and the cross-shard determinism test are still to come |
| M4 workload and telemetry | partial | arrival heterogeneity and telemetry delay built; multi-turn sessions with think time since U22 (dcf77c8); request trace sampling in the engine since U24 (3a350b1); trace replay since U35 (8092332, 20:43); no prefix model |
| M5 policies | partial | SLO classes in the engine since U25a (b852dba, 21:01), the report side U25b; eight routing policies, both forecasting families Issao asked for at 16:22 shipped (`forecast_load` U40a 9ee9841 20:48, `forecast_latency` U40b 052d40e 20:54), and two admission controllers, `deadline_aware` and `fair_share`, behind a one-file-per-policy registry that `build.rs` now generates from the files (289cb22, 120e62d, 210b657, 072a932, 5083b7b); no prefix affinity, no per-decision cost measurement |
| M6 failures | done, extended | retry contrast, finding 6; silent slowdowns and hangs and announced crashes on a scenario schedule since U31a (33dfd18, 21:06) |
| M7 control analysis | not started | |
| M8 dashboard and first deploy | Showcase opens live runs (`lbsim-00009-qb8`, 20:56); `set` steps apply live once U59b lands (U59a, the engine half, is deployed) | static deploy live, scale-to-zero measured (993c03a); `sim-ingress` has leases and the idle guard (d688f8f) but no run or subscription endpoint, though `Sim` is now resumable (fc910f6) and per-sample frames exist (2c4a7eb), which the endpoint needs; `sim-run export` writes runs as the `WIRE.md` JSON documents the stream will carry (41f6435); browser transport merged (389f41e) with mock still the default; the replay source that joins the two is on `master` (U17, 5077a34) and **deployed: since 17:20 <https://lbsim.ai> replays the 30 recorded demo runs with real fleet panels**, revision per `docs/deploy.md` until relayed; **the live server U18 is on `master` (c0e9ea8, 17:23)**: runs start, stream and idle-checkpoint over HTTP and SSE, proven over TCP; per-replica rows and the heatmap on replay are on `master` (U23, ede7b1f); **U28, the web app on that transport, is on `master` (595ea7e, 20:35)**, checked end to end against an in-memory server; the mock-tag classification is mode-aware since U52 (c90bf35, 20:36); **deployed as `lbsim-00006-7sk` at 20:39** (live path verified on the public URL) **and `lbsim-00008-fbz` at 20:49** with the runner; the A/B view and the showcase stay mock until U49 |
| M9 scale validation | not measured | |

22 of the graph's 46 units are done (edbd04a); the arena's judging half scores under rule set v2 with
the catalog append wired (f6a87a9), and request traces exist on the wire against a fixture struct (3a00f92).

## When the dashboard shows real demos

**Since 22:04 on 2026-09-06 the site is `lbsim-00013-mvq` (c22ea56): 12 of 12 dynamics showcased and live, verified by the browser gate on that build locally; the Cloud Run degradation after several runs is the caveat at the top.** History: **it does, since 17:20 PDT, and since 20:39 it also runs them live**: revision `lbsim-00006-7sk` from c90bf35 carried U28, so <https://lbsim.ai/#/dashboard> starts a scenario through the Ingress server and streams it over SSE, verified end to end on the public URL by the main agent at 20:40 (StartRun, GetRun, OpenSubscription, StopRun); **since 20:49, `lbsim-00008-fbz` from c3d1b84 adds the walkthrough runner (U49) and scripts (U48)**, so the Showcase page steps through a recording of each of the nine dynamics that have an exported run, and the dashboard runs the same scenarios live; the Showcase page opening a live run itself landed as U58 and is deployed since `lbsim-00009-qb8` (20:56); it landed at 20:50 (a94693b, deployed 20:56 as `lbsim-00009-qb8`), `set` steps applying live wait on U59a/U59b, and the graph counts, three ways per main, **10 showcased, 10 live-runnable, 10 showcased-live of 11** at 67a9f47, the eleventh on recordings since U60 per the tech lead (demo 11, scripted at 20:51) pending the tech lead's count. Before that: <https://lbsim.ai/runs/index.json> serves the 30 recorded demo runs and
the load-test dashboard at `/` plays them: load, throughput, latency, imbalance and KV are the engine's
numbers; preemptions, wasted GPU, prefix hits and memory tiers are NaN because they are not simulated
yet; the per-replica table and heatmap fill from `replicas.jsonl` once the image is rebuilt with U23
(ede7b1f, after the 17:20 deploy); every other panel still carries its `mock` tag until U28 removes
them panel by panel. The ten demos are also HTML reports at
<https://lbsim.ai/reports/1-routing.html> through `10-probes.html`. Verified from the public URL by the
main agent at 17:20 and by housekeeping at 17:22; revision `lbsim-00005-dzk` from image `:6897543`.
Housekeeping's URL check is one request per round, so the idle measurement stays meaningful.

`docs/dashboard-plan.md` (16c28ac) is the breakdown of why the first dashboard with real runs is
estimated at 19:00 and what can be pulled in; its estimate table moves only on word from the tech lead
or the main agent. Three things stand between it and real data: an Ingress endpoint that runs a scenario and streams
metrics over the JSON/SSE wire in `crates/sim-ingress/WIRE.md`, or until then the pre-baked runs that
`sim-run export --demos` now writes (41f6435), loaded by a replay source; the browser transport client, merged at 16:14 (389f41e) but not yet the default, replacing the
mock engine; and a rebuild and redeploy, measured at
1m22s. The shortest path, asked of the tech lead by the main agent: pre-baked run output in wire format
served statically, so every panel shows real data before the live path exists.

What remains between this and *"all selected dynamics live demoable in the dashboard and in the showcase
page"*: the runner that plays the showcase scripts (U49; the scripts U48 landed at 20:38, U28, the web app
on the live transport, at 20:35, the server U18 and the per-replica rows U23 at 17:23), then a rebuild and deploy; all
in flight or queued in `docs/execution-graph.md`, whose status line counts the dynamics live and showcased. The 15:55 ETA table
(19:00 pre-baked, 21:00 live, 22:30 deployed) is superseded: the pre-baked step landed at 17:20.

## Assumptions being acted on

- Package B of `docs/scope-today.md` plus item 7 is today's scope; the dashboard and the arena were
  reinstated by Issao and are built.
- Arena: SLA cap 0.95 is Issao's rule for now, and the objective is goodput as a share of offered
  work, agreed at 16:12; the code still scores the raw objective until the tech lead switches it. The
  policy generator may write code, not only parameters.
- Prefix-sharing topology comes from a session model with fork-off and merge-back rates, swept.
- Leaf shards are separate processes; memory tiers are Ingress-owned with a seeded bloom-filter
  residency hint. Both are design of record, `docs/ARCHITECTURE.md` §14, not yet built.
- `lbsim.ai` is dark: its DNS zone went with `lbsim-prod`. `TASKS.md` item 1.
- Deployment scales to zero within a replica budget of ten, public at `lbsim.ai` since 16:16. Scale-to-zero is measured, not assumed: two revisions
  reached zero instances within minutes of losing traffic, per `docs/deploy.md` (993c03a), and
  `deploy.sh --check-idle` re-measures it.
- Reference hardware is a 70-billion-parameter model on eight H100s.
- Everything in `docs/ARCHITECTURE.md` section 14 stands as recorded there.

## Tree state

Every agent works in its own worktree beside the repo (`/home/agents/repo/lbsim-<name>`) on a
`claude/<topic>` branch. Subagents push their branch and report; they never merge or push `master`.
The tech lead, the main agent and the housekeeping agent rebase onto `master`, merge `--no-ff` and
push. The rules in full are in `CLAUDE.md`; where this file and `CLAUDE.md` differ, `CLAUDE.md` wins.
