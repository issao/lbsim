[package]
name = "queue-bench"
version = "0.1.0"
edition = "2021"
publish = false

# Standalone measurement of docs/ARCHITECTURE.md open risk 1. No dependencies: the point is
# to measure data structures, and a dependency would only add noise.
[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
debug = false
